@model IEnumerable<Lab7WebApi.Models.Equipments>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_Equipment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nameEquipment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Appointment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ID_Equipment)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.nameEquipment)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Appointment)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.ID_Equipment }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.ID_Equipment }, new { onclick = "return confirm('Видалити ці дані?')" })
    </td>
</tr>
}

</table>
